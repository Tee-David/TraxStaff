# secrets
.env
.env.*
!.env.example

# credentials file (duplicate of the nomarc one) — never commit
important.md
IMPORTANT.md

# working docs / reference screenshots — never commit
plans/

# local agent guidance — kept out of the repo deliberately
CLAUDE.md

# agent tooling — machine-local, not part of the product. These were committed
# once by an over-broad `git add -A` (~12.7k lines of skill definitions); the
# tracked copies still need removing with `git rm -r --cached`.
.claude/
.agents/
skills-lock.json

# node
node_modules/
.pnpm-store/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# next.js
.next/
out/
web/frontend/.next/
web/frontend/out/

# rust / tauri
target/
desktop/src-tauri/target/

# build output
dist/
build/

# editor / OS
.DS_Store
.vscode/
*.swp

# misc
.cache/
coverage/

# TS incremental build info
*.tsbuildinfo
